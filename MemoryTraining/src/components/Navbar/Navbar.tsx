import { StyledAppBar, StyledToolbar, StyledButton} from "./NavbarStyles";

export const Navbar: React.FC = () => {
    return (
      <StyledAppBar>
        <StyledToolbar>
            <StyledButton>
                test
            </StyledButton>
        </StyledToolbar>
      </StyledAppBar>
    );
  };
  